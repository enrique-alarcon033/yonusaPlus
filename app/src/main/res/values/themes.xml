<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.PaniaguaInstaladores" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/black25</item>
        <item name="colorPrimaryDark">@color/clear</item>
        <item name="colorAccent">@color/black25</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="Theme.PaniaguaInstaladores.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.PaniaguaInstaladores.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.PaniaguaInstaladores.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>